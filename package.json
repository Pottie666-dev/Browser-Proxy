{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "mobile:prebuild": "pnpm --filter @workspace/browser-app exec expo prebuild --platform android",
    "mobile:android:native": "pnpm --filter @workspace/browser-app exec expo run:android",
    "mobile:doctor": "pnpm --filter @workspace/browser-app exec expo doctor",
    "mobile:typecheck": "pnpm --filter @workspace/browser-app typecheck",
    "api:typecheck": "pnpm --filter @workspace/api-server typecheck",
    "doctor:browser-proxy": "node scripts/browser-proxy-doctor.mjs"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  }
}
