kind = "mobile"
previewPath = "/"
title = "BrowserVault"
version = "1.0.0"
id = "artifacts/browser-app"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/" ]
localPort = 20112

[services.development]
run = "pnpm --filter @workspace/browser-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/browser-app", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/browser-app", "run", "serve" ]

[services.env]
PORT = "20112"
BASE_PATH = "/"
